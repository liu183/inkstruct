feat: Write 区去掉场景框 + AI 命令专业参数化 + 节拍生成面板

- Write 正文区:去除场景横幅(标题/POV/地点/时间/基调等已在左侧导航),
  正文只保留纯净的 contentEditable 富文本编辑器 + AI 命令按钮组 +
  AI 流式输出区
- AI 命令:点击后弹出专业参数面板,用户调整参数后才生成,避免一键直接输出
  通用参数:输出长度、视角与人称、文风、用户额外要求、参考正文预览
- 生成节拍专用面板:展示参考内容、参数(节拍数量/节奏密度/关注点多选)、
  流式节拍预览,用户点「同意追加到导航节拍」才写入
- aiEngine:扩展 ctx 接受 length/povTense/style/userHint/beatCount/
  beatDensity/beatFocus 参数,buildOutput 据此动态生成

新流程:点 AI 按钮 -> 调整专业参数 -> 看参考内容 -> 点生成 ->
AI 流式输出 -> 完成时选择追加/替换/丢弃,节拍则在面板内显示预览
列表,确认追加才更新导航节拍