feat: Write 写作工作台改为章级视图 + 修复新建项目 bug + Plan 增删改查

- Write:恢复原 contentEditable 编辑器,本章所有场景连续排版,场景间用
  横幅分隔标识;左侧新增「本章结构」导航(场景来自 Plan 规划,列出全部
  场景与节拍),每个节拍点 ✨ 可一键 AI 扩写并插入该场景正文(带「◆ 节拍N·摘要」)
- aiEngine:新增 ai-beat-expand 命令模板,服务于节拍级扩写
- store:修复 firstIds 返回字段名错误(新建/切换项目后导航 id 悬空的根因);
  新增 moveChapter 支持章节拖拽排序与跨单元移动
- Plan:GridView/OutlineView 场景支持增删改查(「添加场景」走 SceneEditModal
  填写标题/POV/地点/摘要后创建并进入写作;卡片 hover 显示编辑/删除);
  章节支持拖拽重排(同单元内 + 跨单元)
- 项目切换器:MetaForm 加 key 强制重置,避免状态复用
